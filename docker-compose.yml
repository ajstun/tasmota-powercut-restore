services:
  tasmota_corrected_exporter:
    image: ghcr.io/ajstun/tasmota-powercut-restore:latest
    restart: always
    volumes:
      - ./data:/data
      - ./config.yaml:/app/config.yaml:ro
    ports:
      - "9500:9500"
